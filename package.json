@@ .. @@
   "scripts": {
-    "test": "echo \"Error: no test specified\" && exit 1"
+    "test": "echo \"Error: no test specified\" && exit 1",
+    "dev": "live-server --port=8000 --host=0.0.0.0",
+    "start": "live-server --port=8000 --host=0.0.0.0"
   },